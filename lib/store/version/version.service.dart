import 'package:flutter_github_releases_service/flutter_github_releases_service.dart';

class VersionService {
  GithubReleasesService grs = GithubReleasesService(
    owner: 'januwA',
    repo: 'flutter_note_app',
  );
}
